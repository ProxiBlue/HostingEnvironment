[PHP]
; Security hardening
expose_php = Off
allow_url_include = Off
session.cookie_httponly = 1
session.cookie_secure = 1
session.cookie_samesite = Lax
session.use_strict_mode = 1

; Disable dangerous functions in FPM context
; Note: CLI is unaffected as this is loaded via php.d for all SAPIs
; Remove exec/shell_exec if deploy scripts need them
disable_functions = passthru,proc_open,popen

; Restrict PHP file access to Magento root and tmp
open_basedir = /var/www/webroot/ROOT:/tmp:/var/www/webroot/site_report

; Performance - important for Magento's deep directory tree
realpath_cache_size = 10M
realpath_cache_ttl = 7200
max_input_vars = 75000
